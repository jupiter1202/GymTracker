---
phase: 02-exercise-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/androidTest/java/de/jupiter1202/gymtracker/feature/exercises/ExerciseDaoTest.kt
  - app/src/test/java/de/jupiter1202/gymtracker/feature/exercises/ExerciseRepositoryTest.kt
autonomous: true
requirements: [EXER-01, EXER-02, EXER-03]

must_haves:
  truths:
    - "Test scaffold files exist and compile against the contracts they will test"
    - "ExerciseDaoTest covers search/filter behaviors and seeded exercise count"
    - "ExerciseRepositoryTest covers delete-guard business logic"
  artifacts:
    - path: "app/src/androidTest/java/de/jupiter1202/gymtracker/feature/exercises/ExerciseDaoTest.kt"
      provides: "Instrumented DAO tests for EXER-01, EXER-02, EXER-03"
      contains: "ExerciseDaoTest"
    - path: "app/src/test/java/de/jupiter1202/gymtracker/feature/exercises/ExerciseRepositoryTest.kt"
      provides: "Unit tests for delete-guard logic (EXER-02)"
      contains: "ExerciseRepositoryTest"
  key_links:
    - from: "ExerciseDaoTest"
      to: "ExerciseDao"
      via: "in-memory Room database (Room.inMemoryDatabaseBuilder)"
      pattern: "inMemoryDatabaseBuilder"
    - from: "ExerciseRepositoryTest"
      to: "ExerciseRepository.deleteExercise"
      via: "mock/fake DAO countUsagesInSessions"
      pattern: "countUsagesInSessions"
---

<objective>
Create Wave 0 test scaffolds — failing skeleton tests that define the behavior contracts for ExerciseDao and ExerciseRepository before their implementations exist.

Purpose: The Nyquist rule requires test files to exist before Wave 2 implementation tasks can have verified automated commands. These scaffolds lock in the expected behavior as compilable, runnable (failing) tests.
Output: Two test files with @Ignore-annotated test stubs that compile against the yet-to-be-created DAO/Repository interfaces.
</objective>

<execution_context>
@/home/florian/.claude/get-shit-done/workflows/execute-plan.md
@/home/florian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-exercise-library/02-CONTEXT.md
@.planning/phases/02-exercise-library/02-RESEARCH.md

<interfaces>
<!-- Existing test patterns to follow exactly -->

From app/src/androidTest/java/de/jupiter1202/gymtracker/core/database/GymTrackerDatabaseTest.kt:
```kotlin
@RunWith(AndroidJUnit4::class)
class GymTrackerDatabaseTest {
    private lateinit var db: GymTrackerDatabase

    @Before
    fun createDb() {
        db = Room.inMemoryDatabaseBuilder(
            ApplicationProvider.getApplicationContext(),
            GymTrackerDatabase::class.java
        ).build()
    }

    @After
    fun closeDb() { db.close() }
    // tests use db.openHelper.readableDatabase or DAO methods
}
```

From app/src/main/java/de/jupiter1202/gymtracker/core/database/entities/Exercise.kt:
```kotlin
@Entity(tableName = "exercises")
data class Exercise(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val name: String,
    @ColumnInfo(name = "primary_muscle_group") val primaryMuscleGroup: String,
    @ColumnInfo(name = "equipment_type") val equipmentType: String,
    @ColumnInfo(name = "is_custom") val isCustom: Boolean = false
)
```

<!-- Wave 2 will create these — tests reference them by contract -->
ExerciseDao (to be created in 02-02):
- fun searchExercises(query: String, muscleGroup: String?): Flow<List<Exercise>>
- suspend fun insert(exercise: Exercise): Long
- suspend fun update(exercise: Exercise)
- suspend fun delete(exercise: Exercise)
- suspend fun countUsagesInSessions(exerciseId: Long): Int

ExerciseRepository (to be created in 02-02):
- suspend fun deleteExercise(exercise: Exercise): DeleteResult
  where DeleteResult = sealed class { object Deleted; data class Blocked(val sessionCount: Int) }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ExerciseDaoTest scaffold (instrumented)</name>
  <files>app/src/androidTest/java/de/jupiter1202/gymtracker/feature/exercises/ExerciseDaoTest.kt</files>
  <behavior>
    - searchExercises("", null) returns all inserted exercises (covers EXER-01 "all results")
    - searchExercises("bench", null) returns only exercises whose name contains "bench" (case-insensitive) (EXER-03)
    - searchExercises("", "Chest") returns only Chest exercises (EXER-03)
    - searchExercises("press", "Chest") returns only Chest exercises containing "press" (EXER-03 combined filter)
    - insert + searchExercises("", null) includes new exercise (EXER-02)
    - countUsagesInSessions returns 0 for an exercise with no workout_sets rows (EXER-02 guard precondition)
    - All seeded exercises (if seed db used) have isCustom = false (EXER-01) — stub with @Ignore until seed db exists
  </behavior>
  <action>
Create the file at the path above. Follow the exact pattern from GymTrackerDatabaseTest.kt:
- Package: de.jupiter1202.gymtracker.feature.exercises
- @RunWith(AndroidJUnit4::class)
- @Before createDb() uses Room.inMemoryDatabaseBuilder with GymTrackerDatabase
- @After closeDb()
- Tests call db.exerciseDao() — this method doesn't exist yet on GymTrackerDatabase; annotate the whole class with @Ignore("Wave 0 scaffold — ExerciseDao not yet implemented") so it compiles but skips at runtime
- Import: androidx.room.Room, androidx.test.core.app.ApplicationProvider, de.jupiter1202.gymtracker.core.database.GymTrackerDatabase, de.jupiter1202.gymtracker.core.database.entities.Exercise
- Write test methods for each behavior item above. Use kotlinx.coroutines.test.runTest for suspend calls.
- Test for seed count (@Ignore individually: "seed db not yet present") — the test should call searchExercises("", null) and assert result.size >= 100
- All other tests insert test data manually using dao.insert() in @Before or within each test
- Use kotlinx.coroutines.runBlocking to collect Flow values in tests (pattern: runBlocking { dao.searchExercises(...).first() })
- Do NOT use mockk or any mocking library — this is an instrumented test using real in-memory Room
  </action>
  <verify>
    <automated>MISSING — ExerciseDao not yet created (Wave 2). Class is @Ignored so ./gradlew testDebugUnitTest passes. After Wave 2: ./gradlew connectedDebugAndroidTest</automated>
  </verify>
  <done>File exists, compiles cleanly (no unresolved references aside from exerciseDao() which triggers a compile error until Wave 2 — accept this; the file IS the contract). The class-level @Ignore prevents runtime failure. ./gradlew assembleDebug passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ExerciseRepositoryTest scaffold (unit)</name>
  <files>app/src/test/java/de/jupiter1202/gymtracker/feature/exercises/ExerciseRepositoryTest.kt</files>
  <behavior>
    - deleteExercise(exercise) where countUsagesInSessions returns 0 → returns DeleteResult.Deleted (EXER-02)
    - deleteExercise(exercise) where countUsagesInSessions returns 3 → returns DeleteResult.Blocked(sessionCount=3) (EXER-02)
    - deleteExercise(exercise) with count > 0 does NOT call dao.delete() (EXER-02, guard enforcement)
  </behavior>
  <action>
Create the file at the path above:
- Package: de.jupiter1202.gymtracker.feature.exercises
- Standard JUnit4 (NOT AndroidJUnit4 — this is a unit test in src/test/)
- Use a hand-written fake DAO (not mockk): create a private inner class FakeExerciseDao that implements ExerciseDao, with a configurable usageCount: Int field
- The fake returns usageCount from countUsagesInSessions() and tracks whether delete() was called via a deleteCalled: Boolean flag
- Follow the SettingsRepository pattern: no constructor injection ceremony, just new FakeExerciseDao() in @Before
- Annotate class with @Ignore("Wave 0 scaffold — ExerciseRepository not yet implemented") so it skips until Wave 2
- Import: org.junit.Test, org.junit.Before, org.junit.Ignore, kotlinx.coroutines.test.runTest
- Use runTest { } for all suspend function calls
- Assert on the sealed class result type using assertTrue/assertEquals (no assertThat — no Hamcrest beyond standard JUnit4)
  </action>
  <verify>
    <automated>MISSING — ExerciseRepository not yet created (Wave 2). Class is @Ignored. ./gradlew testDebugUnitTest passes (skips). After Wave 2: ./gradlew testDebugUnitTest</automated>
  </verify>
  <done>File exists, class-level @Ignore in place, ./gradlew assembleDebug passes.</done>
</task>

</tasks>

<verification>
./gradlew assembleDebug — BUILD SUCCESSFUL with both test files present
./gradlew testDebugUnitTest — passes (ExerciseRepositoryTest skipped via @Ignore)
No unresolved reference errors outside of the expected exerciseDao() and ExerciseRepository references (which will be resolved in Wave 2)
</verification>

<success_criteria>
Both test scaffold files exist on disk. Build compiles. Tests are @Ignored at class level so CI doesn't fail. The behavior contracts for ExerciseDao and ExerciseRepository are locked in as code.
</success_criteria>

<output>
After completion, create .planning/phases/02-exercise-library/02-01-SUMMARY.md
</output>
