---
phase: 02-exercise-library
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified: []
autonomous: false
requirements: [EXER-01, EXER-02, EXER-03]

must_haves:
  truths:
    - "On first install, Exercises tab shows 100-150 exercises grouped by muscle group"
    - "Searching by name filters the list reactively as user types"
    - "Tapping a muscle group chip collapses all other groups"
    - "Search and chip filter work simultaneously"
    - "FAB opens create sheet; created custom exercise appears in list with Custom badge"
    - "Long-press on custom exercise shows Edit and Delete options"
    - "Editing a custom exercise pre-fills and saves correctly"
    - "Deleting an exercise used in sessions shows the block message"
    - "Deleting an unused custom exercise shows confirm dialog and removes it from the list"
  artifacts:
    - path: "app/src/main/java/de/jupiter1202/gymtracker/feature/exercises/ExercisesScreen.kt"
      provides: "Fully functional exercise library screen"
    - path: "app/src/main/assets/gymtracker_seed.db"
      provides: "Pre-seeded SQLite with 100-150 exercises"
  key_links:
    - from: "Exercises tab"
      to: "seed database"
      via: "Room createFromAsset on fresh install"
      pattern: "createFromAsset"
    - from: "Custom exercise"
      to: "ExerciseRepository.deleteExercise"
      via: "delete guard → Blocked dialog or confirm + delete"
      pattern: "deleteExercise"
---

<objective>
Human verification of the complete Phase 2 exercise library feature. All automated tests must be green before this checkpoint. This plan pauses execution for the user to interact with the feature end-to-end on a real device or emulator.

Purpose: Confirms the full user journey works — not just the code compiling. Visual layout, interaction flow, and edge cases (delete guard, empty search results) can only be verified by a human.
Output: Phase 2 approved or issues documented for gap closure.
</objective>

<execution_context>
@/home/florian/.claude/get-shit-done/workflows/execute-plan.md
@/home/florian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-exercise-library/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite before human verification</name>
  <files></files>
  <action>
Run the complete test suite to confirm all automated checks pass before asking the user to verify:

1. Unit tests: ./gradlew testDebugUnitTest
   Expected: ExerciseRepositoryTest passes (3 tests: Deleted, Blocked, no-delete-when-blocked)

2. Instrumented tests: ./gradlew connectedDebugAndroidTest
   Expected: ExerciseDaoTest passes (all cases: seed count >= 100, search by name, filter by muscle group, combined filter, insert+query, countUsagesInSessions)

3. Build verification: ./gradlew assembleDebug
   Expected: BUILD SUCCESSFUL, no warnings about deprecated APIs

If any test fails, STOP and fix before proceeding to Task 2.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest && ./gradlew connectedDebugAndroidTest && ./gradlew assembleDebug</automated>
  </verify>
  <done>All three commands exit with BUILD SUCCESSFUL / tests passed. Zero test failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end exercise library smoke test</name>
  <files></files>
  <action>Human verification checkpoint: install a fresh build and run the 9 scenarios in how-to-verify. No files are modified by this task.</action>
  <verify><automated>./gradlew connectedDebugAndroidTest</automated></verify>
  <done>All 9 verification scenarios pass; user types "approved"</done>
  <what-built>
Complete exercise library: 100-150 seeded exercises displayed in a grouped, searchable, filterable list. Custom exercise creation and editing via bottom sheet. Long-press context menu for custom exercises. Delete guard for exercises used in sessions.
  </what-built>
  <how-to-verify>
Install a FRESH build (uninstall first to test seed data loading):
```
adb uninstall de.jupiter1202.gymtracker
./gradlew installDebug
```

Then test each scenario:

**1. Seed data loads on first launch (EXER-01)**
- Navigate to Exercises tab
- Confirm: exercise list shows grouped sections (Chest, Back, Shoulders...) with sticky headers
- Confirm: at least 100 exercises are visible by scrolling through the list
- Confirm: exercises show name (large text) and "MuscleGroup · EquipmentType" subtitle

**2. Search by name (EXER-03)**
- Tap the search bar and type "bench"
- Confirm: list switches to flat mode (no section headers)
- Confirm: only exercises with "bench" in the name appear
- Confirm: a clear (X) button appears in the search bar
- Tap X — confirm: list resets to grouped mode

**3. Filter by muscle group chip (EXER-03)**
- Tap the "Chest" chip
- Confirm: chip is visually selected
- Confirm: only Chest section remains visible (grouped mode)
- Tap "All" chip — confirm: all sections return

**4. Search + chip combined (EXER-03)**
- Tap "Back" chip, then type "row" in search
- Confirm: flat list showing only Back exercises containing "row"
- Clear search — confirm: grouped mode returns with only Back section

**5. Empty results state**
- Type "xyznotexist" in search
- Confirm: list shows an empty state message (not a blank white screen)
- Clear search

**6. Create custom exercise (EXER-02)**
- Tap the FAB (+)
- Confirm: bottom sheet opens with "New Exercise" title
- Leave name blank and tap Save — confirm: error indicator shown ("Name is required")
- Enter name "My Test Exercise", select "Biceps" from muscle group dropdown, select "Dumbbell" from equipment dropdown
- Tap Save
- Confirm: bottom sheet closes, exercise appears in the Biceps section with a "Custom" badge

**7. Edit custom exercise (EXER-02)**
- Long-press "My Test Exercise"
- Confirm: context menu appears with "Edit exercise" and "Delete exercise" options
- Tap "Edit exercise"
- Confirm: bottom sheet opens pre-filled with "My Test Exercise", "Biceps", "Dumbbell"
- Change name to "My Edited Exercise" and tap Save
- Confirm: exercise appears with updated name in the list

**8. Delete custom exercise (unused) (EXER-02)**
- Long-press "My Edited Exercise" → "Delete exercise"
- Confirm: a confirmation dialog appears (not the "blocked" message)
- Confirm deletion
- Confirm: exercise disappears from the list

**9. kg/lbs settings still work (regression)**
- Navigate to Settings tab → confirm unit toggle still functional
- Navigate back to Exercises — confirm list still renders correctly
  </how-to-verify>
  <resume-signal>Type "approved" if all 9 scenarios pass. If any scenario fails, describe which step failed and what you observed — e.g., "Scenario 6: bottom sheet doesn't open when FAB is tapped".</resume-signal>
</task>

</tasks>

<verification>
All automated tests green before human checkpoint.
Human approves all 9 scenarios covering EXER-01, EXER-02, EXER-03.
</verification>

<success_criteria>
Phase 2 is complete when:
1. On first launch, 100-150 seeded exercises are visible grouped by muscle group (EXER-01)
2. Search filters reactively; chip filters by muscle group; both work simultaneously (EXER-03)
3. User can create, edit, and delete custom exercises with correct guard behavior (EXER-02)
4. Human smoke test approved with no outstanding issues
</success_criteria>

<output>
After completion, create .planning/phases/02-exercise-library/02-05-SUMMARY.md
</output>
