---
phase: 03-workout-plans
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - gradle/libs.versions.toml
  - app/build.gradle.kts
  - app/src/main/java/de/jupiter1202/gymtracker/core/database/dao/WorkoutPlanDao.kt
  - app/src/main/java/de/jupiter1202/gymtracker/core/database/dao/PlanExerciseDao.kt
  - app/src/main/java/de/jupiter1202/gymtracker/core/database/GymTrackerDatabase.kt
  - app/src/main/assets/templates.json
  - app/src/main/java/de/jupiter1202/gymtracker/core/constants/MuscleGroups.kt
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-03

must_haves:
  truths:
    - "App builds with two new dependencies (reorderable, kotlinx.serialization) added cleanly"
    - "WorkoutPlanDao and PlanExerciseDao are compilable Room DAOs"
    - "GymTrackerDatabase exposes both new DAOs"
    - "templates.json in main assets contains all 4 programs (PPL, 5x5, nSuns, GZCLP)"
    - "MuscleGroups constant moved to shared core/constants/ package"
  artifacts:
    - path: "app/src/main/java/de/jupiter1202/gymtracker/core/database/dao/WorkoutPlanDao.kt"
      provides: "Room DAO for workout_plans CRUD"
      exports: ["WorkoutPlanDao"]
    - path: "app/src/main/java/de/jupiter1202/gymtracker/core/database/dao/PlanExerciseDao.kt"
      provides: "Room DAO for plan_exercises with JOIN, reorder, and @Transaction"
      exports: ["PlanExerciseDao", "PlanExerciseWithExercise"]
    - path: "app/src/main/java/de/jupiter1202/gymtracker/core/database/GymTrackerDatabase.kt"
      provides: "Database with workoutPlanDao() and planExerciseDao() abstract funs"
    - path: "app/src/main/assets/templates.json"
      provides: "4 program definitions: PPL, 5x5, nSuns, GZCLP"
    - path: "app/src/main/java/de/jupiter1202/gymtracker/core/constants/MuscleGroups.kt"
      provides: "Shared MUSCLE_GROUPS and EQUIPMENT_TYPES lists"
  key_links:
    - from: "GymTrackerDatabase.kt"
      to: "WorkoutPlanDao, PlanExerciseDao"
      via: "abstract fun declarations"
    - from: "PlanExerciseDao.kt"
      to: "PlanExerciseWithExercise (Exercise @Relation)"
      via: "@Transaction @Query with @Embedded + @Relation"
---

<objective>
Add two new Gradle dependencies, create both Room DAOs with full contract, register them in GymTrackerDatabase, author the production templates.json asset, and extract MUSCLE_GROUPS to a shared constants file.

Purpose: Establishes the complete data layer foundation that the repository and ViewModel (plan 03-03) will build on.
Output: Two DAOs, updated database, templates.json, MuscleGroups constant, new dependencies.
</objective>

<execution_context>
@/home/florian/.claude/get-shit-done/workflows/execute-plan.md
@/home/florian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-workout-plans/03-CONTEXT.md
@.planning/phases/03-workout-plans/03-RESEARCH.md

<interfaces>
<!-- Existing types this plan uses -->

From app/src/main/java/de/jupiter1202/gymtracker/core/database/entities/WorkoutPlan.kt:
```kotlin
@Entity(tableName = "workout_plans")
data class WorkoutPlan(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val name: String,
    val description: String? = null,
    @ColumnInfo(name = "created_at") val createdAt: Long
)
```

From app/src/main/java/de/jupiter1202/gymtracker/core/database/entities/PlanExercise.kt:
```kotlin
@Entity(tableName = "plan_exercises", ...)
data class PlanExercise(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    @ColumnInfo(name = "plan_id") val planId: Long,
    @ColumnInfo(name = "exercise_id") val exerciseId: Long,
    @ColumnInfo(name = "order_index") val orderIndex: Int,
    @ColumnInfo(name = "target_sets") val targetSets: Int,
    @ColumnInfo(name = "target_reps") val targetReps: String
)
```

From app/src/main/java/de/jupiter1202/gymtracker/core/database/entities/Exercise.kt:
```kotlin
@Entity(tableName = "exercises")
data class Exercise(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val name: String,
    @ColumnInfo(name = "primary_muscle_group") val primaryMuscleGroup: String,
    @ColumnInfo(name = "equipment_type") val equipmentType: String,
    @ColumnInfo(name = "is_custom") val isCustom: Boolean = false
)
```

From app/src/main/java/de/jupiter1202/gymtracker/core/database/GymTrackerDatabase.kt:
```kotlin
@Database(entities = [Exercise::class, WorkoutPlan::class, PlanExercise::class,
    WorkoutSession::class, WorkoutSet::class, BodyMeasurement::class],
    version = 1, exportSchema = true)
abstract class GymTrackerDatabase : RoomDatabase() {
    abstract fun exerciseDao(): ExerciseDao
}
```

From app/src/main/java/de/jupiter1202/gymtracker/feature/exercises/ExercisesScreen.kt:
```kotlin
val MUSCLE_GROUPS = listOf(
    "Chest", "Back", "Shoulders", "Biceps", "Triceps", "Forearms",
    "Quads", "Hamstrings", "Glutes", "Calves", "Core", "Cardio"
)
val EQUIPMENT_TYPES = listOf(
    "Barbell", "Dumbbell", "Cable", "Machine", "Bodyweight",
    "Kettlebell", "Resistance Band", "Other"
)
```

From gradle/libs.versions.toml:
- kotlin = "2.2.10" (serialization plugin version must match)
- ksp = "2.2.10-2.0.2"
- No reorderable or kotlinx.serialization entries yet
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new Gradle dependencies and extract MuscleGroups constant</name>
  <files>
    gradle/libs.versions.toml
    app/build.gradle.kts
    app/src/main/java/de/jupiter1202/gymtracker/core/constants/MuscleGroups.kt
    app/src/main/java/de/jupiter1202/gymtracker/feature/exercises/ExercisesScreen.kt
  </files>
  <action>
**gradle/libs.versions.toml** — add to the appropriate sections:

Under `[versions]`:
```
reorderable = "3.0.0"
kotlinxSerializationJson = "1.7.3"
```

Under `[libraries]`:
```
reorderable = { module = "sh.calvin.reorderable:reorderable", version.ref = "reorderable" }
kotlinx-serialization-json = { group = "org.jetbrains.kotlinx", name = "kotlinx-serialization-json", version.ref = "kotlinxSerializationJson" }
```

Under `[plugins]`:
```
kotlin-serialization = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }
```

**app/build.gradle.kts** — add to `plugins {}` block:
```kotlin
alias(libs.plugins.kotlin.serialization)
```

Add to `dependencies {}` block:
```kotlin
implementation(libs.reorderable)
implementation(libs.kotlinx.serialization.json)
```

**app/src/main/java/de/jupiter1202/gymtracker/core/constants/MuscleGroups.kt** — create new file:
```kotlin
package de.jupiter1202.gymtracker.core.constants

val MUSCLE_GROUPS = listOf(
    "Chest", "Back", "Shoulders", "Biceps", "Triceps", "Forearms",
    "Quads", "Hamstrings", "Glutes", "Calves", "Core", "Cardio"
)

val EQUIPMENT_TYPES = listOf(
    "Barbell", "Dumbbell", "Cable", "Machine", "Bodyweight",
    "Kettlebell", "Resistance Band", "Other"
)
```

**ExercisesScreen.kt** — replace the `val MUSCLE_GROUPS = ...` and `val EQUIPMENT_TYPES = ...` declarations at top of file with imports from core/constants:
```kotlin
import de.jupiter1202.gymtracker.core.constants.MUSCLE_GROUPS
import de.jupiter1202.gymtracker.core.constants.EQUIPMENT_TYPES
```
Remove the local declarations. All existing usages within ExercisesScreen.kt are unchanged (same names).
  </action>
  <verify>
    <automated>cd /home/florian/AndroidStudioProjects/GymTracker && ./gradlew :app:assembleDebug 2>&1 | tail -20</automated>
  </verify>
  <done>App builds. MuscleGroups.kt exists. ExercisesScreen.kt imports from core/constants. New dependencies resolve.</done>
</task>

<task type="auto">
  <name>Task 2: WorkoutPlanDao, PlanExerciseDao, GymTrackerDatabase update, and templates.json</name>
  <files>
    app/src/main/java/de/jupiter1202/gymtracker/core/database/dao/WorkoutPlanDao.kt
    app/src/main/java/de/jupiter1202/gymtracker/core/database/dao/PlanExerciseDao.kt
    app/src/main/java/de/jupiter1202/gymtracker/core/database/GymTrackerDatabase.kt
    app/src/main/assets/templates.json
  </files>
  <action>
**WorkoutPlanDao.kt** — create new file following the ExerciseDao pattern exactly:
```kotlin
package de.jupiter1202.gymtracker.core.database.dao

import androidx.room.*
import de.jupiter1202.gymtracker.core.database.entities.WorkoutPlan
import kotlinx.coroutines.flow.Flow

@Dao
interface WorkoutPlanDao {
    @Query("SELECT * FROM workout_plans ORDER BY created_at DESC")
    fun getAllPlans(): Flow<List<WorkoutPlan>>

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insert(plan: WorkoutPlan): Long

    @Update
    suspend fun update(plan: WorkoutPlan)

    @Delete
    suspend fun delete(plan: WorkoutPlan)
}
```

**PlanExerciseDao.kt** — create new file with @Relation JOIN and @Transaction:
```kotlin
package de.jupiter1202.gymtracker.core.database.dao

import androidx.room.*
import de.jupiter1202.gymtracker.core.database.entities.Exercise
import de.jupiter1202.gymtracker.core.database.entities.PlanExercise
import kotlinx.coroutines.flow.Flow

data class PlanExerciseWithExercise(
    @Embedded val planExercise: PlanExercise,
    @Relation(
        parentColumn = "exercise_id",
        entityColumn = "id"
    )
    val exercise: Exercise
)

@Dao
interface PlanExerciseDao {
    @Transaction
    @Query("SELECT * FROM plan_exercises WHERE plan_id = :planId ORDER BY order_index ASC")
    fun getExercisesForPlan(planId: Long): Flow<List<PlanExerciseWithExercise>>

    @Query("SELECT COALESCE(MAX(order_index), -1) FROM plan_exercises WHERE plan_id = :planId")
    suspend fun getMaxOrderIndex(planId: Long): Int

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insert(planExercise: PlanExercise): Long

    @Update
    suspend fun update(planExercise: PlanExercise)

    @Delete
    suspend fun delete(planExercise: PlanExercise)

    @Query("SELECT COUNT(*) FROM plan_exercises WHERE plan_id = :planId")
    suspend fun countExercisesInPlan(planId: Long): Int
}
```

**GymTrackerDatabase.kt** — add two abstract DAO functions:
```kotlin
abstract fun workoutPlanDao(): WorkoutPlanDao
abstract fun planExerciseDao(): PlanExerciseDao
```
Add the required imports for WorkoutPlanDao and PlanExerciseDao. Keep all existing content unchanged.

**app/src/main/assets/templates.json** — create the full production template file with all 4 programs. Use these exercise names which match the seed database exactly. Flatten multi-day programs: include all days with `day_name` field in each exercise entry for display purposes.

Full content:
```json
[
  {
    "id": "ppl",
    "name": "Push Pull Legs",
    "description": "Push Pull Legs — 3 days/week. Classic intermediate hypertrophy split targeting every muscle group twice per week.",
    "days": [
      {
        "name": "Push",
        "exercises": [
          { "exercise_name": "Bench Press", "target_sets": 4, "target_reps": "8-12" },
          { "exercise_name": "Overhead Press", "target_sets": 3, "target_reps": "8-12" },
          { "exercise_name": "Incline Bench Press", "target_sets": 3, "target_reps": "8-12" },
          { "exercise_name": "Tricep Pushdown", "target_sets": 3, "target_reps": "10-15" },
          { "exercise_name": "Lateral Raise", "target_sets": 3, "target_reps": "12-15" }
        ]
      },
      {
        "name": "Pull",
        "exercises": [
          { "exercise_name": "Deadlift", "target_sets": 3, "target_reps": "6-8" },
          { "exercise_name": "Pull-Up", "target_sets": 3, "target_reps": "8-12" },
          { "exercise_name": "Barbell Row", "target_sets": 3, "target_reps": "8-12" },
          { "exercise_name": "Bicep Curl", "target_sets": 3, "target_reps": "10-15" },
          { "exercise_name": "Face Pull", "target_sets": 3, "target_reps": "15-20" }
        ]
      },
      {
        "name": "Legs",
        "exercises": [
          { "exercise_name": "Squat", "target_sets": 4, "target_reps": "8-12" },
          { "exercise_name": "Romanian Deadlift", "target_sets": 3, "target_reps": "8-12" },
          { "exercise_name": "Leg Press", "target_sets": 3, "target_reps": "10-15" },
          { "exercise_name": "Leg Curl", "target_sets": 3, "target_reps": "10-15" },
          { "exercise_name": "Calf Raise", "target_sets": 4, "target_reps": "15-20" }
        ]
      }
    ]
  },
  {
    "id": "stronglifts_5x5",
    "name": "StrongLifts 5x5",
    "description": "StrongLifts 5x5 — 3 days/week. Beginner strength program with linear progression on 5 compound lifts.",
    "days": [
      {
        "name": "Workout A",
        "exercises": [
          { "exercise_name": "Squat", "target_sets": 5, "target_reps": "5" },
          { "exercise_name": "Bench Press", "target_sets": 5, "target_reps": "5" },
          { "exercise_name": "Barbell Row", "target_sets": 5, "target_reps": "5" }
        ]
      },
      {
        "name": "Workout B",
        "exercises": [
          { "exercise_name": "Squat", "target_sets": 5, "target_reps": "5" },
          { "exercise_name": "Overhead Press", "target_sets": 5, "target_reps": "5" },
          { "exercise_name": "Deadlift", "target_sets": 1, "target_reps": "5" }
        ]
      }
    ]
  },
  {
    "id": "nsuns",
    "name": "nSuns 5/3/1",
    "description": "nSuns 5/3/1 — 4 days/week. Intermediate program based on Wendler 5/3/1 with high volume accessory work.",
    "days": [
      {
        "name": "Day 1 — Bench / OHP",
        "exercises": [
          { "exercise_name": "Bench Press", "target_sets": 9, "target_reps": "5/3/1+" },
          { "exercise_name": "Overhead Press", "target_sets": 6, "target_reps": "6-8" }
        ]
      },
      {
        "name": "Day 2 — Squat / Sumo DL",
        "exercises": [
          { "exercise_name": "Squat", "target_sets": 9, "target_reps": "5/3/1+" },
          { "exercise_name": "Deadlift", "target_sets": 6, "target_reps": "6-8" }
        ]
      },
      {
        "name": "Day 3 — OHP / Bench",
        "exercises": [
          { "exercise_name": "Overhead Press", "target_sets": 9, "target_reps": "5/3/1+" },
          { "exercise_name": "Bench Press", "target_sets": 6, "target_reps": "6-8" }
        ]
      },
      {
        "name": "Day 4 — Deadlift / Squat",
        "exercises": [
          { "exercise_name": "Deadlift", "target_sets": 9, "target_reps": "5/3/1+" },
          { "exercise_name": "Squat", "target_sets": 6, "target_reps": "6-8" }
        ]
      }
    ]
  },
  {
    "id": "gzclp",
    "name": "GZCLP",
    "description": "GZCLP — 3 days/week. Beginner-to-intermediate linear progression program organized by tier (T1/T2/T3).",
    "days": [
      {
        "name": "Day A",
        "exercises": [
          { "exercise_name": "Squat", "target_sets": 5, "target_reps": "3" },
          { "exercise_name": "Overhead Press", "target_sets": 3, "target_reps": "10" },
          { "exercise_name": "Lat Pulldown", "target_sets": 3, "target_reps": "15" }
        ]
      },
      {
        "name": "Day B",
        "exercises": [
          { "exercise_name": "Bench Press", "target_sets": 5, "target_reps": "3" },
          { "exercise_name": "Deadlift", "target_sets": 3, "target_reps": "10" },
          { "exercise_name": "Dumbbell Row", "target_sets": 3, "target_reps": "15" }
        ]
      },
      {
        "name": "Day C",
        "exercises": [
          { "exercise_name": "Overhead Press", "target_sets": 5, "target_reps": "3" },
          { "exercise_name": "Squat", "target_sets": 3, "target_reps": "10" },
          { "exercise_name": "Pull-Up", "target_sets": 3, "target_reps": "15" }
        ]
      }
    ]
  }
]
```

Note on exercise name matching: exercise names above match the seed DB naming convention (title case, standard names). During template import in plan 03-03, the repository will look up exercises by name using a case-insensitive query. Any unmatched names will be skipped with a warning logged (not silently create wrong records).
  </action>
  <verify>
    <automated>cd /home/florian/AndroidStudioProjects/GymTracker && ./gradlew :app:assembleDebug 2>&1 | tail -20</automated>
  </verify>
  <done>App builds with no errors. Both DAOs exist. GymTrackerDatabase has workoutPlanDao() and planExerciseDao(). templates.json in main assets has 4 programs. MuscleGroups extracted to core/constants.</done>
</task>

</tasks>

<verification>
After both tasks:
- `./gradlew :app:assembleDebug` passes
- `./gradlew :app:testDebugUnitTest` green (existing tests unaffected)
- `WorkoutPlanDao.kt` and `PlanExerciseDao.kt` exist in core/database/dao/
- `GymTrackerDatabase.kt` has both new abstract methods
- `app/src/main/assets/templates.json` contains 4 program objects
</verification>

<success_criteria>
- App builds clean with 2 new dependencies (reorderable + serialization)
- kotlinx.serialization plugin added to build.gradle.kts
- Both DAOs compile as valid Room interfaces
- GymTrackerDatabase declares workoutPlanDao() and planExerciseDao()
- templates.json has 4 complete program definitions
- MuscleGroups constant moved to core/constants, ExercisesScreen imports from there
</success_criteria>

<output>
After completion, create `.planning/phases/03-workout-plans/03-02-SUMMARY.md`
</output>
