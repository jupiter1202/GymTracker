---
phase: 03-workout-plans
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/test/java/de/jupiter1202/gymtracker/feature/plans/WorkoutPlanRepositoryTest.kt
  - app/src/test/java/de/jupiter1202/gymtracker/feature/plans/PlanExerciseRepositoryTest.kt
  - app/src/test/java/de/jupiter1202/gymtracker/feature/plans/TemplateParserTest.kt
  - app/src/test/assets/templates.json
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-03

must_haves:
  truths:
    - "All three test files compile and tests are initially skipped/pass with no real implementations yet"
    - "Wave 0 establishes the Nyquist test scaffold before any production code is written"
  artifacts:
    - path: "app/src/test/java/de/jupiter1202/gymtracker/feature/plans/WorkoutPlanRepositoryTest.kt"
      provides: "Unit test stubs for PLAN-01 (create/delete plan) and PLAN-02 (importTemplate)"
    - path: "app/src/test/java/de/jupiter1202/gymtracker/feature/plans/PlanExerciseRepositoryTest.kt"
      provides: "Unit test stubs for PLAN-03 (addExercise, removeExercise, reorderExercises)"
    - path: "app/src/test/java/de/jupiter1202/gymtracker/feature/plans/TemplateParserTest.kt"
      provides: "Unit test for PLAN-02 JSON parsing (4 programs returned)"
    - path: "app/src/test/assets/templates.json"
      provides: "Minimal JSON test fixture with 2 programs for parser tests"
  key_links:
    - from: "WorkoutPlanRepositoryTest.kt"
      to: "WorkoutPlanRepository (to be created in 03-03)"
      via: "inline stub types until real class exists"
    - from: "TemplateParserTest.kt"
      to: "app/src/test/assets/templates.json"
      via: "reads JSON from test resources"
---

<objective>
Create Wave 0 test scaffolds for Phase 3. Tests compile immediately using inline fake/stub types. Real repository classes (created in plan 03-03) will replace the stubs.

Purpose: Establish the Nyquist test infrastructure before any production code so every subsequent task has automated verification.
Output: Three test files + one test fixture that all compile and pass (skipped/trivial) before real implementations exist.
</objective>

<execution_context>
@/home/florian/.claude/get-shit-done/workflows/execute-plan.md
@/home/florian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-workout-plans/03-CONTEXT.md
@.planning/phases/03-workout-plans/03-VALIDATION.md

<interfaces>
<!-- Existing patterns to mirror exactly -->

From app/src/test/java/de/jupiter1202/gymtracker/feature/exercises/ExerciseRepositoryTest.kt:
- Uses inline `FakeExerciseDao : ExerciseDao` inner class
- No imports of production classes — self-contained fake pattern
- `runTest {}` from kotlinx-coroutines-test
- JUnit 4: `@Before`, `@Test`, `org.junit.Assert.*`

Package convention: de.jupiter1202.gymtracker.feature.plans

Test directory: app/src/test/java/de/jupiter1202/gymtracker/feature/plans/
Test assets directory: app/src/test/assets/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WorkoutPlanRepositoryTest and PlanExerciseRepositoryTest stubs</name>
  <files>
    app/src/test/java/de/jupiter1202/gymtracker/feature/plans/WorkoutPlanRepositoryTest.kt
    app/src/test/java/de/jupiter1202/gymtracker/feature/plans/PlanExerciseRepositoryTest.kt
  </files>
  <action>
Create both test files using inline stub types so they compile without any production code existing yet. Use the EXACT same pattern as ExerciseRepositoryTest.kt: inline fake DAO inner class, JUnit 4, runTest.

**WorkoutPlanRepositoryTest.kt** — define the following at file scope (no imports from production code except data entities which already exist):
- Stub interface `FakeWorkoutPlanDao` implementing a minimal `WorkoutPlanDao` contract: `insert(plan): Long`, `update(plan)`, `delete(plan)`, `getAllPlans(): Flow<List<WorkoutPlan>>`
- Stub interface `FakePlanExerciseDao` implementing: `insert(pe): Long`, `update(pe)`, `delete(pe)`, `getExercisesForPlan(planId): Flow<List<Any>>`, `getMaxOrderIndex(planId): Int`
- Stub class `WorkoutPlanRepository(planDao, planExerciseDao)` with methods: `createPlan(name, description?): Long`, `deletePlan(plan)`, `importTemplate(templatePlan, exercises)`
- Tests (initially just compile and pass trivially):
  - `createPlan_returnsValidId()` — calls `repository.createPlan("Test", null)`, asserts result > 0
  - `deletePlan_isAlwaysAllowed()` — calls `repository.deletePlan(plan)`, asserts no exception thrown
  - `importTemplate_createsWorkoutPlanAndExerciseRows()` — calls `repository.importTemplate(...)`, asserts planId > 0

**PlanExerciseRepositoryTest.kt** — define stub types at file scope:
- Tests:
  - `addExercise_appendsWithCorrectOrderIndex()` — asserts orderIndex = maxIndex + 1
  - `removeExercise_deletesCorrectRow()` — asserts delete called on correct item
  - `reorderExercises_updatesAllOrderIndexValues()` — asserts all items updated in order

All stubs live in the test file itself (no separate production files needed). The stub `WorkoutPlanRepository` and `WorkoutPlanDao` defined here will be REPLACED by real production classes in plan 03-03. At that point, update these test files to import the real production types and remove the stubs.

Use package: `de.jupiter1202.gymtracker.feature.plans`
Import only: `kotlinx.coroutines.flow.Flow`, `kotlinx.coroutines.flow.flowOf`, `kotlinx.coroutines.test.runTest`, `org.junit.*`, production entities `WorkoutPlan`, `PlanExercise`, `Exercise` (these already exist in Phase 1).
  </action>
  <verify>
    <automated>cd /home/florian/AndroidStudioProjects/GymTracker && ./gradlew :app:testDebugUnitTest --tests "de.jupiter1202.gymtracker.feature.plans.*" 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files compile and all tests pass (using inline stubs). No production repository classes required.</done>
</task>

<task type="auto">
  <name>Task 2: TemplateParserTest stub and test fixture</name>
  <files>
    app/src/test/java/de/jupiter1202/gymtracker/feature/plans/TemplateParserTest.kt
    app/src/test/assets/templates.json
  </files>
  <action>
Create the JSON fixture and a minimal test that validates JSON structure. The test reads raw JSON from a string (no Android Context needed) so it runs as a pure JVM unit test.

**app/src/test/assets/templates.json** — minimal fixture with exactly 2 programs (PPL + StrongLifts 5x5) following the schema from RESEARCH.md:
```json
[
  {
    "id": "ppl",
    "name": "Push Pull Legs",
    "description": "Push Pull Legs — 3 days/week. Classic hypertrophy split.",
    "days": [
      {
        "name": "Push",
        "exercises": [
          { "exercise_name": "Bench Press", "target_sets": 4, "target_reps": "8-12" }
        ]
      }
    ]
  },
  {
    "id": "stronglifts_5x5",
    "name": "StrongLifts 5x5",
    "description": "StrongLifts 5x5 — 3 days/week. Beginner strength program.",
    "days": [
      {
        "name": "Workout A",
        "exercises": [
          { "exercise_name": "Squat", "target_sets": 5, "target_reps": "5" }
        ]
      }
    ]
  }
]
```

**TemplateParserTest.kt** — define a stub `TemplateProgram` data class + `parseTemplates(json: String): List<TemplateProgram>` function locally in the test file. This will be replaced by real `kotlinx.serialization` types in plan 03-02/03-03. For now, use `org.json.JSONArray` (available on JVM without any extra dep) to parse.

Tests:
- `parseTemplates_returnsTwoPrograms()` — reads FIXTURE_JSON const, calls parseTemplates, asserts size == 2
- `parseTemplates_firstProgramIsPPL()` — asserts programs[0].id == "ppl"
- `parseTemplates_secondProgramIs5x5()` — asserts programs[1].id == "stronglifts_5x5"

Use a hardcoded JSON constant in the test class (copy of the fixture) — this avoids needing file I/O in unit tests.

Use package: `de.jupiter1202.gymtracker.feature.plans`
  </action>
  <verify>
    <automated>cd /home/florian/AndroidStudioProjects/GymTracker && ./gradlew :app:testDebugUnitTest --tests "de.jupiter1202.gymtracker.feature.plans.TemplateParserTest" 2>&1 | tail -20</automated>
  </verify>
  <done>TemplateParserTest compiles and all 3 parser tests pass using inline stub types and JSONArray. app/src/test/assets/templates.json exists with 2 programs.</done>
</task>

</tasks>

<verification>
Full test run after Wave 0:
`./gradlew :app:testDebugUnitTest` — all existing tests plus new stubs must be green.
</verification>

<success_criteria>
- WorkoutPlanRepositoryTest.kt exists and compiles with 3 tests passing
- PlanExerciseRepositoryTest.kt exists and compiles with 3 tests passing
- TemplateParserTest.kt exists and compiles with 3 tests passing
- app/src/test/assets/templates.json exists with valid JSON (2 programs)
- `./gradlew :app:testDebugUnitTest` is green
</success_criteria>

<output>
After completion, create `.planning/phases/03-workout-plans/03-01-SUMMARY.md`
</output>
