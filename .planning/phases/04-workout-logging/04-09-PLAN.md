---
phase: 04-workout-logging
plan: 09
type: execute
wave: 1
depends_on:
  - 04-06
files_modified:
  - app/src/main/java/de/julius1202/gymtracker/feature/workout/WorkoutLoggingViewModel.kt
autonomous: true
gap_closure: true
requirements:
  - LOG-01

must_haves:
  truths:
    - "Crash recovery loads Exercise entities from database for in-progress sessions"
    - "Logged sets are grouped by exercise and displayed with exercise names"
  artifacts:
    - path: "app/src/main/java/de/julius1202/gymtracker/feature/workout/WorkoutLoggingViewModel.kt"
      provides: "resumeSession() loads Exercise entities and creates ExerciseSection objects"
  key_links:
    - from: "resumeSession(sessionId)"
      to: "Exercise database lookup"
      via: "exerciseRepository.getExerciseById() per logged set"
      pattern: "getExerciseById|Exercise.*JOIN"
    - from: "resumeSession()"
      to: "_exerciseSections StateFlow"
      via: "Create ExerciseSection per exercise"
      pattern: "_exerciseSections.value = sections"
---

<objective>
Fix GAPS-02 (actually a sub-gap of GAPS-01): Load Exercise entities during crash recovery so resumed sessions show exercises with their names and logged sets grouped by exercise.

Purpose: When app restarts with an in-progress session, resumeSession() must load Exercise entities from the database and create ExerciseSection objects. Currently it returns empty exerciseSections, blocking crash recovery verification.

Output: resumeSession() loads Exercise data and re-constructs ExerciseSection objects with logged sets grouped by exercise.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-workout-logging/04-CONTEXT.md
@.planning/phases/04-workout-logging/04-VERIFICATION.md (see "Gap 2: Crash Recovery Returns Empty Workout")
@.planning/phases/04-workout-logging/04-03-SUMMARY.md (WorkoutLoggingViewModel implementation context)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement full Exercise loading in resumeSession()</name>
  <files>app/src/main/java/de/julius1202/gymtracker/feature/workout/WorkoutLoggingViewModel.kt</files>
  <action>
In WorkoutLoggingViewModel.kt, find the resumeSession(sessionId: Long) function (currently at line ~206-234). Replace the entire function body to:

1. Load the WorkoutSession record by sessionId using sessionRepository
2. Set _activeSession.value = session to restore active session
3. Load all WorkoutSet records for the session using setRepository.getSetsForSession(sessionId)
4. For each unique exerciseId in the logged sets:
   - Load the Exercise entity using exerciseRepository.getExerciseById(exerciseId) 
   - NOTE: If ExerciseRepository doesn't have getExerciseById(), add it or use a DAO query
5. For each Exercise, create an ExerciseSection containing:
   - exercise: the Exercise object
   - loggedSets: WorkoutSet records filtered for this exercise (converted to LoggedSet format with setNumber, weight, reps)
   - pendingInput: fresh PendingSetInput("", "")
   - previousPerformance: call formatPreviousPerformance() with sets from prior sessions
6. Set _exerciseSections.value = sections (the list of constructed ExerciseSection objects)
7. Call startElapsedTimer(session.startedAt) to resume timer
8. Wrap in try/catch with Timber.e() logging on error

Reference the pattern from 04-VERIFICATION.md Gap 2 Fix section. Replace the stub comment "(Simplified - full version would need Exercise entities from DB)" with real implementation.

Key: Do NOT return an empty sections list — this is the root cause blocking crash recovery.
  </action>
  <verify>
    <automated>grep -n "resumeSession" app/src/main/java/de/julius1202/gymtracker/feature/workout/WorkoutLoggingViewModel.kt | head -5</automated>
  </verify>
  <done>
resumeSession() loads Exercise entities from database and creates ExerciseSection objects with logged sets grouped by exercise.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify exerciseRepository provides getExerciseById method</name>
  <files>app/src/main/java/de/jupiter1202/gymtracker/core/data/repository/ExerciseRepository.kt</files>
  <action>
Verify that ExerciseRepository has a public function to fetch a single Exercise by ID. The function should be:
- Name: getExerciseById(exerciseId: Long): Exercise? or suspend fun returning Flow<Exercise?>
- Behavior: Queries the Exercise table by id, returns the Exercise record or null if not found

If the function doesn't exist, add it:
```kotlin
suspend fun getExerciseById(id: Long): Exercise? {
    return withContext(Dispatchers.IO) {
        exerciseDao.getExerciseById(id)
    }
}
```

And add the corresponding DAO query if needed (in ExerciseDao.kt):
```kotlin
@Query("SELECT * FROM exercises WHERE id = :id LIMIT 1")
suspend fun getExerciseById(id: Long): Exercise?
```

If the function already exists, no changes needed — resumeSession() can call it directly.
  </action>
  <verify>
    <automated>grep -n "getExerciseById" app/src/main/java/de/jupiter1202/gymtracker/core/data/repository/ExerciseRepository.kt</automated>
  </verify>
  <done>
ExerciseRepository provides a getExerciseById(id: Long) function that returns a single Exercise from the database.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database query → ViewModel | Exercise data fetched from trusted Room database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09-01 | Tampering | Exercise lookup | mitigate | Room queries enforce schema validation; no user input in query |
</threat_model>

<verification>
- resumeSession() loads Exercise entities from database
- ExerciseSection objects are created for each exercise in the session
- Logged sets are grouped and displayed per exercise
- Test: Start workout, log a set, force-stop app, reopen and verify exercises and logged sets visible
</verification>

<success_criteria>
- resumeSession() function loads Exercise entities and constructs ExerciseSection objects
- No empty exerciseSections returned — sections list populated with Exercise data
- Crash recovery verification can pass: app resumes with exercises visible
- ExerciseRepository provides getExerciseById() method
</success_criteria>

<output>
After completion, create `.planning/phases/04-workout-logging/04-09-SUMMARY.md`
</output>
