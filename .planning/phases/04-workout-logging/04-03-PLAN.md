---
phase: 04-workout-logging
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - app/src/main/java/de/jupiter1202/gymtracker/feature/workout/WorkoutLoggingViewModel.kt
  - app/src/test/java/de/jupiter1202/gymtracker/feature/workout/WorkoutSessionRepositoryTest.kt
  - app/src/test/java/de/jupiter1202/gymtracker/feature/workout/WorkoutSetRepositoryTest.kt
  - app/src/test/java/de/jupiter1202/gymtracker/feature/workout/WorkoutLoggingViewModelTest.kt
  - app/src/main/java/de/jupiter1202/gymtracker/core/di/AppModule.kt
autonomous: true
requirements:
  - LOG-01
  - LOG-04
  - LOG-05

must_haves:
  truths:
    - "WorkoutLoggingViewModel holds all active session UI state as StateFlow"
    - "logSet() writes to Room immediately (no batching — per D-18)"
    - "Elapsed time derives from currentTimeMillis() - session.startedAt every second"
    - "getPreviousPerformance() returns formatted string e.g. '3×8 @ 80 kg' for each exercise"
    - "All previously failing Wave 0 tests now pass"
  artifacts:
    - path: "app/src/main/java/de/jupiter1202/gymtracker/feature/workout/WorkoutLoggingViewModel.kt"
      provides: "ViewModel: startSession, logSet, addExercise, removeExercise, finishSession, elapsed timer, previous perf"
      exports: ["WorkoutLoggingViewModel", "ActiveSessionUiState", "ExerciseSection", "PendingSetInput", "RestTimerState"]
    - path: "app/src/test/java/de/jupiter1202/gymtracker/feature/workout/WorkoutSessionRepositoryTest.kt"
      provides: "Passing tests: createSession, getActiveSession"
    - path: "app/src/test/java/de/jupiter1202/gymtracker/feature/workout/WorkoutSetRepositoryTest.kt"
      provides: "Passing tests: logSet, getPreviousSessionSets"
    - path: "app/src/test/java/de/jupiter1202/gymtracker/feature/workout/WorkoutLoggingViewModelTest.kt"
      provides: "Passing tests: elapsed time computation"
  key_links:
    - from: "WorkoutLoggingViewModel.kt"
      to: "WorkoutSessionRepository + WorkoutSetRepository"
      via: "constructor injection; registered in AppModule as viewModel {}"
      pattern: "viewModel \\{ WorkoutLoggingViewModel"
    - from: "WorkoutLoggingViewModel._elapsedMs"
      to: "session.startedAt"
      via: "startElapsedTimer() coroutine: _elapsedMs.value = currentTimeMillis() - startedAt"
      pattern: "currentTimeMillis.*startedAt"
---

<objective>
WorkoutLoggingViewModel — all UI state for the active workout screen.

Purpose: Bridges the data layer (Plan 02) with the UI (Plans 04–05). Holds the complete session state as StateFlow: active session, exercise sections with logged sets and pending input rows, elapsed timer (separate flow to avoid full recompose), rest timer state, and previous performance strings. Turns Red tests from Plan 01 Green.

Output: WorkoutLoggingViewModel.kt plus updated test files with real implementations replacing TODO stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-workout-logging/04-CONTEXT.md
@.planning/phases/04-workout-logging/04-RESEARCH.md
@.planning/phases/04-workout-logging/04-01-SUMMARY.md
@.planning/phases/04-workout-logging/04-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 outputs — executor must read these files before implementing -->

WorkoutSessionRepository (app/src/main/java/de/jupiter1202/gymtracker/feature/workout/WorkoutSessionRepository.kt):
```kotlin
class WorkoutSessionRepository(private val dao: WorkoutSessionDao) {
    suspend fun createSession(name: String, planId: Long?): Long
    suspend fun getActiveSession(): WorkoutSession?
    suspend fun getSessionById(id: Long): WorkoutSession?
    suspend fun finishSession(session: WorkoutSession)
}
```

WorkoutSetRepository (app/src/main/java/de/jupiter1202/gymtracker/feature/workout/WorkoutSetRepository.kt):
```kotlin
class WorkoutSetRepository(private val dao: WorkoutSetDao) {
    suspend fun logSet(sessionId: Long, exerciseId: Long, weightKg: Double, reps: Int): Long
    fun getSetsForSession(sessionId: Long): Flow<List<WorkoutSet>>
    fun getSetsForExercise(sessionId: Long, exerciseId: Long): Flow<List<WorkoutSet>>
    suspend fun getPreviousSessionSets(exerciseId: Long): List<WorkoutSet>
    suspend fun deleteSet(set: WorkoutSet)
}
```

Exercise entity (app/src/main/java/de/jupiter1202/gymtracker/core/database/entities/Exercise.kt):
Referenced for ExerciseSection.exercise field.

UnitConverter (app/src/main/java/de/jupiter1202/gymtracker/core/UnitConverter.kt):
```kotlin
object UnitConverter {
    fun kgToLbs(kg: Double): Double = kg * 2.20462
    fun lbsToKg(lbs: Double): Double = lbs / 2.20462
}
```

SettingsRepository (app/src/main/java/de/jupiter1202/gymtracker/feature/settings/SettingsRepository.kt):
```kotlin
val restTimerSeconds: Flow<Int>  // default 90
val weightUnit: Flow<String>     // "kg" or "lbs"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WorkoutLoggingViewModel</name>
  <files>
    app/src/main/java/de/jupiter1202/gymtracker/feature/workout/WorkoutLoggingViewModel.kt
    app/src/main/java/de/jupiter1202/gymtracker/core/di/AppModule.kt
  </files>
  <read_first>
    - app/src/main/java/de/jupiter1202/gymtracker/feature/workout/WorkoutSessionRepository.kt
    - app/src/main/java/de/jupiter1202/gymtracker/feature/workout/WorkoutSetRepository.kt
    - app/src/main/java/de/jupiter1202/gymtracker/feature/settings/SettingsRepository.kt
    - app/src/main/java/de/jupiter1202/gymtracker/core/UnitConverter.kt
    - app/src/main/java/de/jupiter1202/gymtracker/core/di/AppModule.kt
    - app/src/main/java/de/jupiter1202/gymtracker/feature/plans/WorkoutPlanViewModel.kt (Koin DSL pattern)
    - app/src/main/java/de/jupiter1202/gymtracker/core/database/entities/Exercise.kt
  </read_first>
  <behavior>
    ViewModel state and behavior contract:

    Data classes (defined at file scope in WorkoutLoggingViewModel.kt):
    - `data class PendingSetInput(val weightDisplay: String, val reps: String)` — display unit (may be kg or lbs string)
    - `data class LoggedSet(val id: Long, val setNumber: Int, val weightKg: Double, val reps: Int)`
    - `data class ExerciseSection(val exercise: Exercise, val loggedSets: List<LoggedSet>, val pendingInput: PendingSetInput, val previousPerformance: String)`
      - `previousPerformance` format: `"Last: 3×8 @ 80 kg"` or `"Last: 3×8 @ 176.4 lbs"` — empty string if no prior session
    - `sealed class RestTimerState { object Idle; data class Running(val remaining: Int, val total: Int) }`

    ViewModel StateFlows:
    - `val activeSession: StateFlow<WorkoutSession?>` — null when no session active
    - `val exerciseSections: StateFlow<List<ExerciseSection>>` — ordered list of exercise sections
    - `val elapsedMs: StateFlow<Long>` — separate flow (not in a combined UiState — per Pitfall 6)
    - `val restTimerState: StateFlow<RestTimerState>` — Idle or Running
    - `val weightUnit: StateFlow<String>` — "kg" or "lbs" from SettingsRepository

    ViewModel functions:
    - `fun startSession(name: String, planId: Long?, exercises: List<Exercise>)` — creates session, loads previous perf for each exercise, starts elapsed timer
    - `fun resumeSession(sessionId: Long)` — called on crash recovery; loads existing session + sets from Room, starts elapsed timer
    - `fun addExercise(exercise: Exercise)` — appends a new ExerciseSection with empty loggedSets, prefilled pendingInput from previous perf
    - `fun removeExercise(exercise: Exercise)` — removes section from exerciseSections
    - `fun updatePendingWeight(exerciseId: Long, weight: String)` — updates PendingSetInput.weightDisplay for that exercise
    - `fun updatePendingReps(exerciseId: Long, reps: String)` — updates PendingSetInput.reps for that exercise
    - `fun logSet(exerciseId: Long)` — validates inputs via toDoubleOrNull()/toIntOrNull() guards; converts display weight to kg if unit is lbs; writes to WorkoutSetRepository immediately (D-18); updates section with new LoggedSet; adds a new PendingSetInput pre-filled from the just-logged set; starts rest timer
    - `fun deleteSet(set: LoggedSet, exerciseId: Long)` — calls WorkoutSetRepository.deleteSet, removes from section
    - `fun skipRestTimer()` — cancels restTimerJob, sets restTimerState = Idle
    - `fun extendRestTimer(extraSeconds: Int)` — if Running, restart with remaining + extraSeconds
    - `fun finishSession(): Boolean` — calls WorkoutSessionRepository.finishSession; cancels timers; returns false if any section has loggedSets.isEmpty() (caller shows confirmation dialog per D-17); returns true after session saved
    - `fun discardSession()` — deletes the active WorkoutSession row and all sets; sets activeSession = null; cancels timers

    Elapsed timer (per Pattern 3 in RESEARCH.md):
    - `startElapsedTimer(startedAt: Long)` private fun — coroutine in viewModelScope, while(isActive) { _elapsedMs.value = System.currentTimeMillis() - startedAt; delay(1000) }

    Rest timer (per Pattern 4 in RESEARCH.md):
    - `startRestTimer(durationSeconds: Int)` private fun — counts down; on completion calls `triggerTimerAlert()`
    - `triggerTimerAlert()` private fun — vibrates via `(context.getSystemService(Context.VIBRATOR_SERVICE) as Vibrator).vibrate(VibrationEffect.createOneShot(300L, VibrationEffect.DEFAULT_AMPLITUDE))` and plays SoundPool beep

    Previous performance string helper:
    - `formatPreviousPerformance(sets: List<WorkoutSet>, unit: String): String` — if sets.isEmpty() return ""; else group by setNumber, count unique, take last set weight; produce "Last: {count}×{reps} @ {weight} {unit}"; convert kg to lbs if unit=="lbs" via UnitConverter.kgToLbs
  </behavior>
  <action>
    Create WorkoutLoggingViewModel.kt in package `de.jupiter1202.gymtracker.feature.workout`.

    Key implementation notes:
    - Constructor: `class WorkoutLoggingViewModel(private val sessionRepository: WorkoutSessionRepository, private val setRepository: WorkoutSetRepository, private val settingsRepository: SettingsRepository, private val context: android.content.Context) : ViewModel()`
    - Inject `context` via Koin `androidContext()` in AppModule — needed for Vibrator
    - SoundPool: create in init block with `SoundPool.Builder().setMaxStreams(1).build()`. Attempt to load `R.raw.timer_beep` if the resource exists. Use `OnLoadCompleteListener` to set a `soundLoaded: Boolean` flag. If resource not found, catch the exception silently (vibration-only fallback).
    - SoundPool release: `override fun onCleared() { soundPool.release(); super.onCleared() }`
    - weightUnit: `settingsRepository.weightUnit.stateIn(viewModelScope, SharingStarted.WhileSubscribed(5_000), "kg")`
    - restTimerDuration: collected once via `settingsRepository.restTimerSeconds` when rest timer starts
    - For `logSet()`: input validation: `val kg = if (unit == "lbs") UnitConverter.lbsToKg(weightDisplay.toDoubleOrNull() ?: return) else weightDisplay.toDoubleOrNull() ?: return`; `val repsInt = reps.toIntOrNull() ?: return`; require `kg >= 0.0` and `repsInt >= 1` else return

    Update AppModule.kt — add after existing viewModel blocks:
    ```kotlin
    viewModel { WorkoutLoggingViewModel(get(), get(), get(), androidContext()) }
    ```
    Add import: `de.jupiter1202.gymtracker.feature.workout.WorkoutLoggingViewModel`

    IMPORTANT: `elapsedMs` must be a SEPARATE StateFlow<Long>, not nested inside a combined UiState object. This prevents the entire ActiveWorkoutScreen from recomposing every second (per Pitfall 6).
  </action>
  <verify>
    <automated>cd /home/florian/AndroidStudioProjects/GymTracker && ./gradlew :app:assembleDebug 2>&1 | grep -E "error:|BUILD" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - WorkoutLoggingViewModel.kt contains `class WorkoutLoggingViewModel` extending ViewModel
    - WorkoutLoggingViewModel.kt contains `val elapsedMs: StateFlow<Long>` as a standalone field (not inside a data class)
    - WorkoutLoggingViewModel.kt contains `val restTimerState: StateFlow<RestTimerState>`
    - WorkoutLoggingViewModel.kt contains `fun logSet(exerciseId: Long)` with `toDoubleOrNull()` and `toIntOrNull()` guards
    - WorkoutLoggingViewModel.kt contains `fun startRestTimer` private function with `VibrationEffect.createOneShot`
    - WorkoutLoggingViewModel.kt contains `override fun onCleared()` calling `soundPool.release()`
    - AppModule.kt contains `viewModel { WorkoutLoggingViewModel(get(), get(), get(), androidContext()) }`
    - `./gradlew :app:assembleDebug` output contains "BUILD SUCCESSFUL"
  </acceptance_criteria>
  <done>WorkoutLoggingViewModel.kt compiles. ViewModel registered in AppModule. App builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Turn Wave 0 tests Green (replace stubs with real implementations)</name>
  <files>
    app/src/test/java/de/jupiter1202/gymtracker/feature/workout/WorkoutSessionRepositoryTest.kt
    app/src/test/java/de/jupiter1202/gymtracker/feature/workout/WorkoutSetRepositoryTest.kt
    app/src/test/java/de/jupiter1202/gymtracker/feature/workout/WorkoutLoggingViewModelTest.kt
  </files>
  <read_first>
    - app/src/test/java/de/jupiter1202/gymtracker/feature/workout/WorkoutSessionRepositoryTest.kt (current stub state)
    - app/src/test/java/de/jupiter1202/gymtracker/feature/workout/WorkoutSetRepositoryTest.kt (current stub state)
    - app/src/test/java/de/jupiter1202/gymtracker/feature/workout/WorkoutLoggingViewModelTest.kt (current stub state)
    - app/src/main/java/de/jupiter1202/gymtracker/feature/workout/WorkoutSessionRepository.kt
    - app/src/main/java/de/jupiter1202/gymtracker/feature/workout/WorkoutSetRepository.kt
  </read_first>
  <action>
    Replace stub classes in each test file with real implementations using Fake DAOs.

    WorkoutSessionRepositoryTest.kt — rewrite:
    - Remove stub WorkoutSessionRepository class
    - Import real `de.jupiter1202.gymtracker.feature.workout.WorkoutSessionRepository`
    - Keep FakeWorkoutSessionDao at file scope — it implements WorkoutSessionDao interface:
      ```kotlin
      class FakeWorkoutSessionDao : WorkoutSessionDao {
          private val sessions = mutableListOf<WorkoutSession>()
          private var nextId = 1L
          override suspend fun insert(session: WorkoutSession): Long {
              val id = nextId++
              sessions.add(session.copy(id = id))
              return id
          }
          override suspend fun getActiveSession(): WorkoutSession? =
              sessions.firstOrNull { !it.isCompleted }
          override suspend fun getById(id: Long): WorkoutSession? =
              sessions.firstOrNull { it.id == id }
          override suspend fun update(session: WorkoutSession) {
              val index = sessions.indexOfFirst { it.id == session.id }
              if (index >= 0) sessions[index] = session
          }
      }
      ```
    - Tests now call real repository. Remove TODO() from test bodies. Tests must pass.

    WorkoutSetRepositoryTest.kt — rewrite:
    - Remove stub WorkoutSetRepository class
    - Import real `de.jupiter1202.gymtracker.feature.workout.WorkoutSetRepository`
    - FakeWorkoutSetDao implements WorkoutSetDao with a list backing store. getPreviousSessionSets() needs access to sessions to check isCompleted — simplify by storing completed session ids in fake, or pass a FakeWorkoutSessionDao reference.
      Simpler approach: FakeWorkoutSetDao stores sets in memory. For getPreviousSessionSets, it filters by exerciseId where the session is completed — since FakeWorkoutSetDao doesn't know sessions, make getPreviousSessionSets accept a helper list of completed session ids via a `var completedSessionIds: Set<Long>` property on the fake. Tests set this before asserting.
    - Tests must pass.

    WorkoutLoggingViewModelTest.kt — rewrite:
    - Remove stub WorkoutLoggingViewModel class
    - Import real `de.jupiter1202.gymtracker.feature.workout.WorkoutLoggingViewModel`
    - For tests that need ViewModel: use `kotlinx.coroutines.test.TestScope` + `Dispatchers.setMain(UnconfinedTestDispatcher())` in @Before
    - For elapsed time test: set `startedAt = System.currentTimeMillis() - 5000L`, call viewModel.resumeSession() with a fake session having that startedAt, wait 100ms, assert elapsedMs.value in 4900..5500 range
    - For activeSession_isNullInitially: assert `viewModel.activeSession.value == null`
    - Tests must pass.
  </action>
  <verify>
    <automated>cd /home/florian/AndroidStudioProjects/GymTracker && ./gradlew :app:testDebugUnitTest --tests "de.jupiter1202.gymtracker.feature.workout.*" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :app:testDebugUnitTest --tests "de.jupiter1202.gymtracker.feature.workout.*"` exits 0
    - Test output contains "BUILD SUCCESSFUL"
    - Test output shows all feature/workout tests passing (no failures)
    - WorkoutSessionRepositoryTest.kt no longer contains any `TODO()` calls
    - WorkoutSetRepositoryTest.kt no longer contains any `TODO()` calls
    - WorkoutLoggingViewModelTest.kt no longer contains any `TODO()` calls
  </acceptance_criteria>
  <done>All three test files pass. No TODO() stubs remain. ./gradlew testDebugUnitTest exits 0 for feature/workout tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → ViewModel.logSet() | User-entered weight (String) and reps (String) cross from UI into ViewModel |
| ViewModel → WorkoutSetRepository | Validated double/int values cross into Room write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | WorkoutLoggingViewModel.logSet() | mitigate | `toDoubleOrNull()` and `toIntOrNull()` parse guards return early on null; require `kg >= 0.0` and `reps >= 1` before calling repository — malformed or empty input is silently dropped (no crash, no corrupt write) |
| T-04-03-02 | Denial of Service | Elapsed timer coroutine | accept | Timer runs in `viewModelScope`; cancelled automatically by `onCleared()` when ViewModel is destroyed — no leak risk |
| T-04-03-03 | Tampering | restTimerDuration from SettingsRepository | mitigate | `setRestTimerSeconds()` has `require(seconds in 10..600)` guard in Plan 02; ViewModel reads the stored value and uses it — out-of-range values cannot be stored |
</threat_model>

<verification>
`./gradlew :app:assembleDebug` exits 0.
`./gradlew :app:testDebugUnitTest --tests "de.jupiter1202.gymtracker.feature.workout.*"` exits 0.
WorkoutLoggingViewModel.kt has separate `elapsedMs: StateFlow<Long>` (grep confirms it is not nested in a data class).
AppModule.kt contains `viewModel { WorkoutLoggingViewModel(get(), get(), get(), androidContext()) }`.
</verification>

<success_criteria>
- WorkoutLoggingViewModel.kt compiles and is registered in Koin
- All Wave 0 tests pass (no TODO stubs remain)
- elapsedMs is a standalone StateFlow (not inside combined UiState)
- logSet() has toDoubleOrNull/toIntOrNull input guards
- App builds clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-workout-logging/04-03-SUMMARY.md`
</output>
