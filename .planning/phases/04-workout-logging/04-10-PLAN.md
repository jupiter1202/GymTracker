---
phase: 04-workout-logging
plan: 10
type: execute
wave: 2
depends_on:
  - 04-06
  - 04-08
  - 04-09
files_modified:
  - app/src/main/java/de/julius1202/gymtracker/feature/workout/WorkoutLoggingViewModel.kt
  - app/src/main/java/de/julius1202/gymtracker/feature/workout/ActiveWorkoutScreen.kt
autonomous: true
gap_closure: true
requirements:
  - LOG-05

must_haves:
  truths:
    - "Elapsed time updates every 1 second during active workout"
    - "Top bar displays elapsed time in real-time format HH:MM:SS"
  artifacts:
    - path: "app/src/main/java/de/julius1202/gymtracker/feature/workout/WorkoutLoggingViewModel.kt"
      provides: "startElapsedTimer() coroutine job emits elapsed time updates every 1 second"
    - path: "app/src/main/java/de/julius1202/gymtracker/feature/workout/ActiveWorkoutScreen.kt"
      provides: "Collects elapsedMs StateFlow and displays in TopAppBar title"
  key_links:
    - from: "startElapsedTimer(sessionStartedAt)"
      to: "_elapsedMs StateFlow"
      via: "Emit elapsed time every 1 second via .emit() or .value ="
      pattern: "_elapsedMs.emit|_elapsedMs.value.*delay"
    - from: "ActiveWorkoutScreen.kt"
      to: "elapsedMs.collectAsStateWithLifecycle()"
      via: "Compose state collection"
      pattern: "collectAsStateWithLifecycle.*elapsedMs"
---

<objective>
Fix GAPS-03: Make elapsed time update in real-time during workout.

Purpose: The startElapsedTimer() coroutine job is initialized but not properly updating the _elapsedMs StateFlow or the updates are not flowing to the UI. This blocks LOG-05 verification (elapsed time displays and updates).

Output: Elapsed time in TopAppBar increments by 1 second every 1 second, starting from session.startedAt.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-workout-logging/04-CONTEXT.md
@.planning/phases/04-workout-logging/04-VERIFICATION.md (see "Gap 3: Elapsed Time Not Updating in UI")
@.planning/phases/04-workout-logging/04-03-SUMMARY.md (WorkoutLoggingViewModel implementation context)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix startElapsedTimer() coroutine job to emit updates</name>
  <files>app/src/main/java/de/julius1202/gymtracker/feature/workout/WorkoutLoggingViewModel.kt</files>
  <action>
In WorkoutLoggingViewModel.kt, find the startElapsedTimer(sessionStartedAt: Long) function (currently at line ~357-369). Verify and fix:

1. The coroutine job loop is correctly calculating elapsed time: val elapsed = System.currentTimeMillis() - sessionStartedAt
2. The StateFlow is being updated with each tick. Replace _elapsedMs.value = elapsed with _elapsedMs.emit(elapsed) to ensure state updates propagate:
   ```kotlin
   private fun startElapsedTimer(sessionStartedAt: Long) {
       if (elapsedTimerJob?.isActive == true) return
       elapsedTimerJob = viewModelScope.launch {
           while (isActive) {
               val now = System.currentTimeMillis()
               val elapsed = now - sessionStartedAt
               _elapsedMs.emit(elapsed)  // Emit every 1000ms
               delay(1000)  // Wait 1 second before next emit
           }
       }
   }
   ```
3. Ensure viewModelScope is valid (should be, as it's a ViewModel scope). If there's a lifecycle issue causing the scope to cancel unexpectedly, add defensive checks:
   - Check isActive inside the loop (already present in the code)
   - Consider using GlobalScope as fallback ONLY if this is a critical path (not recommended, but if essential for crash recovery scenarios)
4. Verify that startElapsedTimer() is actually being called in startSession() around line ~149
5. Verify that resumeSession() also calls startElapsedTimer() after loading the session

Key: Use emit() not .value = to ensure StateFlow subscribers are notified of every tick.
  </action>
  <verify>
    <automated>grep -A 15 "private fun startElapsedTimer" app/src/main/java/de/julius1202/gymtracker/feature/workout/WorkoutLoggingViewModel.kt | grep -E "emit|delay|while"</automated>
  </verify>
  <done>
startElapsedTimer() job emits elapsed time every 1 second via _elapsedMs.emit() while the job is active.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify ActiveWorkoutScreen collects and displays elapsed time</name>
  <files>app/src/main/java/de/julius1202/gymtracker/feature/workout/ActiveWorkoutScreen.kt</files>
  <action>
In ActiveWorkoutScreen.kt, verify the Top App Bar title displays elapsed time correctly:

1. Confirm elapsedMs is collected at the composable level, around line ~46-58:
   ```kotlin
   val elapsed by workoutViewModel.elapsedMs.collectAsStateWithLifecycle()
   ```
2. Verify the TopAppBar title displays formatElapsed(elapsed), e.g.:
   ```kotlin
   TopAppBar(
       title = { Text(formatElapsed(elapsed)) },
       ...
   )
   ```
3. If the collection is missing or using the wrong method (e.g., collect() instead of collectAsStateWithLifecycle()), fix it
4. Verify formatElapsed() function exists and formats milliseconds as "HH:MM:SS" or "H:MM:SS"
   - If missing, add:
   ```kotlin
   private fun formatElapsed(milliseconds: Long): String {
       val totalSeconds = milliseconds / 1000
       val hours = totalSeconds / 3600
       val minutes = (totalSeconds % 3600) / 60
       val seconds = totalSeconds % 60
       return if (hours > 0) {
           String.format("%d:%02d:%02d", hours, minutes, seconds)
       } else {
           String.format("0:%02d:%02d", minutes, seconds)
       }
   }
   ```

The issue likely stemmed from either: (a) missing collection, (b) wrong collection method, or (c) missing formatElapsed() function.
  </action>
  <verify>
    <automated>grep -n "elapsedMs\|collectAsStateWithLifecycle\|formatElapsed" app/src/main/java/de/julius1202/gymtracker/feature/workout/ActiveWorkoutScreen.kt | head -10</automated>
  </verify>
  <done>
ActiveWorkoutScreen.kt collects elapsedMs StateFlow with collectAsStateWithLifecycle() and displays formatted time in TopAppBar title.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify startElapsedTimer is called when session starts and resumes</name>
  <files>app/src/main/java/de/julius1202/gymtracker/feature/workout/WorkoutLoggingViewModel.kt</files>
  <action>
Verify that startElapsedTimer() is called:
1. In startSession() function after creating the WorkoutSession (around line ~149)
   - Expected call: startElapsedTimer(session.startedAt) after session record is created
   - If missing, add it before returning the sessionId
2. In resumeSession() function after loading the session (at the end of the function, after _exerciseSections is set)
   - Expected call: startElapsedTimer(session.startedAt) to resume the timer
   - If missing, add it so crash recovery resumes the timer immediately

If either is missing, add the call. The timer must start on session creation AND resume on app restart.
  </action>
  <verify>
    <automated>grep -n "startElapsedTimer" app/src/main/java/de/julius1202/gymtracker/feature/workout/WorkoutLoggingViewModel.kt</automated>
  </verify>
  <done>
startElapsedTimer() is called in startSession() and resumeSession() to ensure timer starts on session creation and resumes on app restart.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ViewModel StateFlow → UI Composable | Timer updates from coroutine to state collection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10-01 | Denial-of-Service | startElapsedTimer job | mitigate | Job cancelled when ViewModel destroyed; isActive check prevents infinite loops |
</threat_model>

<verification>
- startElapsedTimer() emits elapsed time every 1 second
- ActiveWorkoutScreen.kt collects elapsedMs and displays in TopAppBar
- formatElapsed() function formats milliseconds as "HH:MM:SS"
- Test: Start workout, watch elapsed time increment by 1 second per second for 2+ minutes
</verification>

<success_criteria>
- Elapsed time in TopAppBar increments continuously during active workout
- Timer resumes correctly after crash recovery
- startElapsedTimer() called in both startSession() and resumeSession()
- StateFlow updates propagate to UI via collectAsStateWithLifecycle()
</success_criteria>

<output>
After completion, create `.planning/phases/04-workout-logging/04-10-SUMMARY.md`
</output>
