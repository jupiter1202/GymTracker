---
phase: 04-workout-logging
plan: 11
type: execute
wave: 3
depends_on:
  - 04-08
  - 04-09
  - 04-10
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - LOG-01
  - LOG-03
  - LOG-04
  - LOG-05

must_haves:
  truths:
    - "User can start a workout from a plan and exercises from that plan are shown"
    - "Crash recovery loads Exercise entities and displays them"
    - "Elapsed time updates in real time in the top bar"
    - "All three gaps are closed and verified"
  artifacts: []
  key_links: []
---

<objective>
Gap closure verification — Re-verify all three fixed gaps against running app.

Purpose: After fixing GAPS-01, GAPS-02, and GAPS-03, conduct manual smoke test to confirm all gaps are resolved and Phase 4 can proceed to final human verification.

Output: Gap closure verification report confirming all three gaps fixed.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-workout-logging/04-CONTEXT.md
@.planning/phases/04-workout-logging/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run unit tests to confirm no regressions</name>
  <files></files>
  <action>
Run the full unit test suite to ensure gap closure fixes didn't break anything:
```
./gradlew :app:testDebugUnitTest
```
All tests must pass.
  </action>
  <verify>
    <automated>cd /home/florian/AndroidStudioProjects/GymTracker && ./gradlew :app:testDebugUnitTest 2>&1 | tail -5</automated>
  </verify>
  <done>Unit tests pass with no failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Gap closure verification — 3 scenarios</name>
  <what-built>
    Fixed gaps:
    - GAPS-01: PlansScreen now fetches plan exercises before starting session
    - GAPS-02: resumeSession() loads Exercise entities from database
    - GAPS-03: startElapsedTimer() emits elapsed time updates every 1 second
  </what-built>
  <how-to-verify>
    Install app on device/emulator. Run each scenario:

    **Scenario 1: Start workout from plan — exercises loaded (GAPS-01)**
    1. Go to Plans tab
    2. Tap "Start" on any plan
    3. EXPECTED: Active workout screen opens with exercises from that plan listed in sections
    4. If you see exercises: ✓ GAPS-01 FIXED
    5. If you see empty list: ✗ GAPS-01 NOT FIXED

    **Scenario 2: Crash recovery — exercises persist (GAPS-02)**
    1. Start a workout from any plan
    2. Log at least one set for an exercise
    3. Force-stop the app (Settings → Apps → GymTracker → Force Stop)
    4. Reopen the app
    5. EXPECTED: App auto-navigates to active workout screen with exercises and logged sets visible
    6. If exercises are visible: ✓ GAPS-02 FIXED
    7. If exercises are missing/empty: ✗ GAPS-02 NOT FIXED

    **Scenario 3: Elapsed time increments (GAPS-03)**
    1. Start any workout (from plan or ad-hoc)
    2. Watch the top app bar title
    3. EXPECTED: Title shows elapsed time (e.g. "0:00:15") and increments by 1 second every second
    4. Wait for at least 10 seconds and watch the seconds increment continuously
    5. If time increments every second: ✓ GAPS-03 FIXED
    6. If time is stuck at 0 or doesn't increment: ✗ GAPS-03 NOT FIXED
  </how-to-verify>
  <resume-signal>Type "all-fixed" if all 3 gaps verified as fixed, or describe which gaps are still broken.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human tester → app | Manual verification of gap closure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11-01 | Tampering | Verification scope | accept | Manual verification confirms user-facing behavior only |
</threat_model>

<verification>
All 3 gaps confirmed fixed through manual testing on running app.
</verification>

<success_criteria>
- GAPS-01: Exercises loaded when starting workout from plan
- GAPS-02: Exercises persist after crash recovery
- GAPS-03: Elapsed time increments every second
- All unit tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-workout-logging/04-11-SUMMARY.md`
</output>
