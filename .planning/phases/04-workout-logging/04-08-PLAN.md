---
phase: 04-workout-logging
plan: 08
type: execute
wave: 1
depends_on:
  - 04-06
files_modified:
  - app/src/main/java/de/jupiter1202/gymtracker/feature/plans/PlansScreen.kt
  - app/src/main/java/de/jupiter1202/gymtracker/feature/workout/WorkoutLoggingViewModel.kt
autonomous: true
gap_closure: true
requirements:
  - LOG-01

must_haves:
  truths:
    - "User can start a workout from a plan and see exercises from that plan loaded"
    - "Exercises list is fetched from WorkoutPlanRepository before session starts"
  artifacts:
    - path: "app/src/main/java/de/jupiter1202/gymtracker/feature/plans/PlansScreen.kt"
      provides: "Fetches plan exercises before calling startSessionAndGetId()"
    - path: "app/src/main/java/de/jupiter1202/gymtracker/feature/workout/WorkoutLoggingViewModel.kt"
      provides: "Receives actual exercise list instead of empty list"
  key_links:
    - from: "PlansScreen.kt onStartClick"
      to: "WorkoutPlanRepository.getPlanExercises()"
      via: "Launch coroutine scope"
      pattern: "scope.launch.*getPlanExercises"
    - from: "WorkoutPlanRepository.getPlanExercises()"
      to: "WorkoutLoggingViewModel.startSessionAndGetId()"
      via: "Pass exercises parameter"
      pattern: "startSessionAndGetId.*exercises"
---

<objective>
Fix GAPS-01: Load plan exercises before starting a workout session.

Purpose: When user taps "Start" on a plan card in PlansScreen, the exercises from that plan must be fetched and passed to startSessionAndGetId() instead of an empty list. This unblocks LOG-01 verification.

Output: PlansScreen fetches exercises from WorkoutPlanRepository and passes them to the ViewModel when starting a session.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-workout-logging/04-CONTEXT.md
@.planning/phases/04-workout-logging/04-VERIFICATION.md (see "Gap 1: Plan Exercises Not Loaded")
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch plan exercises in PlansScreen before session start</name>
  <files>app/src/main/java/de/jupiter1202/gymtracker/feature/plans/PlansScreen.kt</files>
  <action>
In PlansScreen.kt, find the onStartClick handler (currently at line ~135) where exercises = emptyList() is passed to startSessionAndGetId(). Replace this entire click handler to:

1. Launch a coroutine scope using the provided scope (from CoroutineScope parameter or LocalViewModelStoreOwner)
2. Fetch plan exercises using planRepository.getPlanExercises(plan.id).first() 
3. Extract the Exercise objects from the PlanExerciseWithExercise wrapper (call .exercise on each item)
4. Call startSessionAndGetId() with the actual exercises list instead of emptyList()
5. Pass the resulting sessionId to onStartPlan(sessionId) for navigation

Error handling: Wrap in try/catch and show a toast/snackbar if getPlanExercises() fails, with fallback to starting empty session or graceful error message.

The fixed code pattern (reference from 04-VERIFICATION.md Gap 1 Fix section, adapted for your context):
```kotlin
scope.launch {
    try {
        val planExercises = planRepository.getPlanExercises(plan.id).first()
            .map { it.exercise }  // Extract Exercise from PlanExerciseWithExercise
        val sessionId = workoutViewModel.startSessionAndGetId(
            name = plan.name,
            planId = plan.id,
            exercises = planExercises  // Pass actual exercises, not emptyList()
        )
        onStartPlan(sessionId)
    } catch (e: Exception) {
        // Show error message (use snackbar or log)
        Timber.e(e, "Failed to fetch plan exercises for plan ${plan.id}")
    }
}
```

Do NOT pass emptyList() — this is the root cause of the gap.
  </action>
  <verify>
    <automated>grep -n "startSessionAndGetId" app/src/main/java/de/jupiter1202/gymtracker/feature/plans/PlansScreen.kt | grep -v "emptyList"</automated>
  </verify>
  <done>
PlansScreen passes actual exercise list to startSessionAndGetId(), fetched from WorkoutPlanRepository.getPlanExercises().
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify startSessionAndGetId receives exercises and creates exercise sections</name>
  <files>app/src/main/java/de/jupiter1202/gymtracker/feature/workout/WorkoutLoggingViewModel.kt</files>
  <action>
Verify that WorkoutLoggingViewModel.startSessionAndGetId() correctly processes the exercises parameter and creates ExerciseSection objects for each exercise. Reference the implementation from 04-02-SUMMARY.md and 04-03-SUMMARY.md.

Check that:
1. startSessionAndGetId() function signature receives exercises: List<Exercise> parameter
2. Function iterates over exercises (lines ~165-186) and creates ExerciseSection per exercise
3. Each ExerciseSection includes: exercise object, empty loggedSets list, pendingInput row, previousPerformance string
4. ExerciseSections are stored in _exerciseSections StateFlow

If any of these are missing or exercises list is not being iterated correctly, fix the loop to ensure sections are created when exercises list is non-empty.

Do NOT change the function signature — the issue is on the PlansScreen side (Task 1 fixes this by passing real exercises instead of emptyList()).
  </action>
  <verify>
    <automated>grep -A 20 "fun startSessionAndGetId" app/src/main/java/de/julius1202/gymtracker/feature/workout/WorkoutLoggingViewModel.kt | grep -E "exercises|ExerciseSection"</automated>
  </verify>
  <done>
startSessionAndGetId() correctly creates ExerciseSection objects for each exercise in the exercises list (as long as the list is not empty).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User tap on PlansScreen → ViewModel | Exercise list fetch validated by repository layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08-01 | Denial-of-Service | getPlanExercises query | accept | Repository enforces query limits; single plan fetch is bounded |
</threat_model>

<verification>
- PlansScreen no longer passes emptyList() to startSessionAndGetId()
- PlansScreen fetches exercises from WorkoutPlanRepository.getPlanExercises()
- startSessionAndGetId() receives non-empty exercises list and creates ExerciseSection objects
- Test: Start a workout from a plan card and verify exercises appear in ActiveWorkoutScreen
</verification>

<success_criteria>
- PlansScreen.kt onStartClick handler fetches exercises before calling startSessionAndGetId()
- No emptyList() passed to startSessionAndGetId() from PlansScreen
- ExerciseSection objects are created for each fetched exercise
- ActiveWorkoutScreen displays exercises when starting a workout from a plan
</success_criteria>

<output>
After completion, create `.planning/phases/04-workout-logging/04-08-SUMMARY.md`
</output>
